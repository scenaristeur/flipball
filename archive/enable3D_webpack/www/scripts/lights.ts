export class Lights  {
  constructor( ) {
  console.log("lights")

  // const { hemisphereLight, ambientLight, directionalLight } = this.lights
  //    hemisphereLight.intensity = 0.65
  //
  //    console.log("lights",this.lights)

  // this.directional = this.lights.directionalLight({ intensity: 3 })
  //        this.directional.position.set(0, 10, 5)
  //        this.lights.helper.directionalLightHelper(this.directional)
  //
  // this.lights.hemisphereLight({ intensity: 0.6 })
  // this.spot = this.lights.spotLight({ /*color: 'blue',*/ angle: Math.PI / 6, intensity: 3  })
  // this.spot.position.set(0, 7, 5)
  // this.spot.rotation.set(0.6, 0,0)
  //     this.spotHelper = this.lights.helper.spotLightHelper(this.spot)
  //     this.point = this.lights.pointLight({ color: 0x00ff7f, intensity: 2, distance: 10 })
  //              this.point.position.set(0, 5, 0)
  //              this.lights.helper.pointLightHelper(this.point)


  }

  // getPoint( t, optionalTarget = new THREE.Vector3() ) {
  //
  //   const tx = t * 3 - 1.5;
  //   const ty = Math.sin( 2 * Math.PI * t );
  //   const tz = 0;
  //
  //   return optionalTarget.set( tx, ty, tz ).multiplyScalar( this.scale );
  //
  // }

}
